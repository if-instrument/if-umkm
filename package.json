{
  "name": "if-instrument-umkm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "find public/scripts -name '*.js' -exec node --check {} \\;",
    "serve": "php83 spark serve --port 8081"
  }
}
